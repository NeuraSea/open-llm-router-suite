apiVersion: v1
kind: Service
metadata:
  name: enterprise-llm-proxy
  namespace: enterprise-llm-proxy
spec:
  selector:
    app: enterprise-llm-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
